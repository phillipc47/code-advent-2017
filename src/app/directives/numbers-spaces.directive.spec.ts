import { NumbersSpacesDirective } from './numbers-spaces.directive';

describe('NumbersSpacesDirective', () => {
  it('should create an instance', () => {
    const directive = new NumbersSpacesDirective();
    expect(directive).toBeTruthy();
  });
});
